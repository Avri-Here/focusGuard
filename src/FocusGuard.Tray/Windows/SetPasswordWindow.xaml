<Window x:Class="FocusGuard.Tray.Windows.SetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FocusGuard — set admin password"
        Width="380" Height="240"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" ShowInTaskbar="True"
        Icon="pack://application:,,,/Resources/AppIcon.ico">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="Choose an admin password. You'll need it to pause, disable, or change the whitelist. There is no recovery — write it down."/>

        <TextBlock Grid.Row="1" Text="New password" Margin="0,0,0,2"/>
        <PasswordBox x:Name="NewPwdBox" Grid.Row="2" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="3" Text="Confirm" Margin="0,0,0,2"/>
        <PasswordBox x:Name="ConfirmPwdBox" Grid.Row="4" Margin="0,0,0,8" VerticalAlignment="Top"/>

        <DockPanel Grid.Row="5">
            <TextBlock x:Name="ErrorText" Foreground="#B00020" VerticalAlignment="Center" TextWrapping="Wrap"/>
            <Button x:Name="SubmitButton" Content="Set password" DockPanel.Dock="Right"
                    Padding="12,4" Click="OnSubmitClick" IsDefault="True"/>
        </DockPanel>
    </Grid>
</Window>
